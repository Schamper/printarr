services:
  printarr:
    build: .
    image: printarr:latest
    container_name: printarr
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-UTC}
    volumes:
      - ./config:/config
    ports:
      - "6969:6969"
    restart: unless-stopped
